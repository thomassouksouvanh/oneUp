{% extends 'base.html.twig' %}

{% block title %}{{category.slug}}{% endblock %}

{% block body %}
    {% include 'partial/header.html.twig'%}
    <div class="card categoryCard mb-3" >
        <div class="row no-gutters">
            <div class="col-md-8">
                <div class="card-body">
                    <h5 class="card-title">{{ category.name }}</h5>
                    <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
                    <hr>
                    <a href="{{ path('category_index') }}">Retour à la liste</a>
                    <hr>
                    <div class="mb-3">
                        <a href="{{ path('category_edit', {'slug': category.slug}) }}">
                            <button class="btn login">Editer</button>
                        </a>
                    </div>

                    {{ include('category/_delete_form.html.twig') }}
                </div>
            </div>
            <div class="col-md-4">
                <img src="https://via.placeholder.com/900x750/O https://placeholder.com/" class="card-img" alt="...">
            </div>
        </div>
    </div>

{% endblock %}
