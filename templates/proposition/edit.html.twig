{% extends 'base.html.twig' %}

{% block title %}Edit Proposition{% endblock %}

{% block body %}
    {% include('partial/header2.html.twig') %}
    <h4>Modifier la proposition {{ proposition.name }}</h4>

    {{ include('proposition/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('proposition_index') }}">Retour à la liste</a>

    {{ include('proposition/_delete_form.html.twig') }}
{% endblock %}
