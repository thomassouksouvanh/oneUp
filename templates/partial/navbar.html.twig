<!--Navbar -->
<nav class="navbar navbar-expand-lg navbar-light mt-3 mb-3">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent-555"
            aria-controls="navbarSupportedContent-555" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent-555">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('homepage') }}"><i class="fas fa-home"></i>
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('proposition_new') }}">Proposer</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('category_new') }}">Ajouter ligne de prod</a>
            </li>
            <li class="nav-item dropdown active">
                <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink-555" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">
                    Catégories
                </a>
                <div class="dropdown-menu dropdown-secondary" aria-labelledby="navbarDropdownMenuLink-555">
                    <a class="dropdown-item" href="#">D10</a>
                    <a class="dropdown-item" href="#">D100</a>
                    <a class="dropdown-item" href="#">IH1000</a>
                    <a class="dropdown-item" href="#">PDR</a>
                    <a class="dropdown-item" href="#">GI</a>
                    <a class="dropdown-item" href="#">QC</a>
                    <a class="dropdown-item" href="#">AUTRE</a>
                </div>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto nav-flex-icons">
{#            <li class="nav-item">
                <a class="nav-link waves-effect waves-light">1
                    <i class="fas fa-envelope"></i>
                </a>
            </li>#}
            <li class="nav-item avatar dropdown ">
                {% if app.user %}
                <a class="nav-link " id="navbarDropdownMenuLink-55" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">
                    {% if app.user.avatar == null %}
                           <img class="avatar-nav-default" src="{{ asset('/build/uploads/userDefault.png') }}" alt="connecté"/>
                            {% else %}
                                <img class="avatar-nav-default" src="{{ asset('/build/uploads/userDefault.png') }}" alt="déconnecté"/>
                                {% endif %}
                </a>
                    {% else %}
                        <a class="nav-link active mt-0" href="{{ path('app_login') }}">Connexion</a>
                    {% endif %}

                <div class="dropdown-menu dropdown-menu-lg-right dropdown-secondary "
                     aria-labelledby="navbarDropdownMenuLink-55">
                    {%  if app.user %}
                        <a class="dropdown-item" href="{{ path('user_show',{'slug':app.user.slug}) }}">Mon profil</a>
                        <a class="dropdown-item" href="#">Mes propositions</a>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                    {% endif %}
                </div>
            </li>
        </ul>
    </div>
</nav>
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
<!--/.Navbar -->
